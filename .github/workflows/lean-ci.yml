name: lean-ci

on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: leanprover/lean-action@v1
        with:
          use-mathlib-cache: true
      - name: Supermanifolds Quality Guard
        run: ./scripts/check_supermanifolds_quality.sh
      - name: Build
        run: lake build
